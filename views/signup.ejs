<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Signup | UniqueBlog</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white">

  <!-- Navbar -->
  <nav class="bg-gray-900 p-4 text-white shadow-md">
    <div class="max-w-7xl mx-auto flex justify-between items-center">
      <a href="/" class="text-2xl font-bold text-indigo-400">UniqueBlog</a>
      <a href="/signin" class="hover:text-indigo-300">Login</a>
    </div>
  </nav>

  <!-- Signup Form -->
  <div class="flex items-center justify-center min-h-screen px-4">
    <div class="bg-gradient-to-br from-gray-800 to-gray-900 p-10 rounded-2xl shadow-2xl w-full max-w-md">
      <h2 class="text-3xl font-bold text-center text-indigo-400 mb-6">Create Account</h2>
      <form action="/signup" method="post" class="space-y-5" >
        
        <input type="text" name="username" placeholder="Username" class="w-full p-3 bg-gray-700 rounded-md text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
        <input type="email" name="email" placeholder="Email" class="w-full p-3 bg-gray-700 rounded-md text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
        <input type="password" name="password" placeholder="Password" class="w-full p-3 bg-gray-700 rounded-md text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
        <button type="submit" class="w-full py-3 bg-indigo-500 hover:bg-indigo-600 rounded-md font-semibold transition">Sign Up</button>
      </form>
      <p class="text-sm text-gray-400 text-center mt-4">Already have an account? <a href="/signin" class="text-indigo-400 hover:underline">Login</a></p>
      <% if (locals.error) { %>
        <div>
         <h4 class="font-semibold text-red-300">Error</h4>
         <p class="text-sm">Welcome —<%= locals.error %></p>
         </div>
         </div>
      <% } %>
  </div>
</body>
</html>
