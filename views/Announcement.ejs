<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Create Announcement | UniqueAdmin</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Quicksand', sans-serif;
      background: linear-gradient(to bottom right, #0f0f12, #1a1a1e);
    }
  </style>
</head>
<body class="text-white min-h-screen flex flex-col">

  <!-- Navbar -->
  <nav class="bg-gray-900 shadow-lg py-4 px-4 sm:px-6 sticky top-0 z-50">
    <div class="flex flex-col sm:flex-row sm:justify-between items-center max-w-6xl mx-auto">
      <h1 class="text-xl font-bold text-indigo-400 flex items-center gap-2 mb-2 sm:mb-0">
        <i class="fas fa-shield-alt text-yellow-400"></i> UniqueAdmin
      </h1>
      <ul class="flex gap-4 text-sm text-gray-300">
        <li><a href="/admin" class="hover:text-indigo-400 transition">Dashboard</a></li>
        <li><a href="/view-announcements" class="hover:text-indigo-400 transition">All Announcements</a></li>
      </ul>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="flex-grow flex items-center justify-center px-4 py-10">
    <div class="w-full max-w-3xl bg-gray-900 p-6 sm:p-8 rounded-2xl shadow-2xl border border-gray-800 transition duration-300 hover:shadow-indigo-600/20">
      <h2 class="text-2xl sm:text-3xl font-bold text-indigo-400 mb-6 flex items-center gap-3 animate-pulse">
        <i class="fas fa-bullhorn text-yellow-400"></i> Send New Announcement
      </h2>

      <form action="/send-announcement" method="POST" class="space-y-6">
        <div>
          <label class="block text-sm text-gray-400 mb-1" for="title">Title</label>
          <input type="text" id="title" name="title" required
            class="w-full px-4 py-2 rounded-xl bg-gray-800 text-white border border-gray-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition"/>
        </div>

        <div>
          <label class="block text-sm text-gray-400 mb-1" for="content">Content</label>
          <textarea id="content" name="content" rows="6" required
            class="w-full px-4 py-3 rounded-xl bg-gray-800 text-white border border-gray-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 transition"></textarea>
        </div>

        <button type="submit"
          class="w-full flex items-center justify-center gap-2 bg-indigo-500 hover:bg-indigo-600 text-white px-6 py-3 rounded-full shadow-md transition-all duration-300 hover:scale-105">
          <i class="fas fa-paper-plane"></i> Send to All Users
        </button>
      </form>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 text-gray-400 text-sm py-4 text-center border-t border-gray-800">
    © <span id="year"></span> UniqueAdmin. Made with ❤️ for awesome announcements.
  </footer>

  <script>
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>
