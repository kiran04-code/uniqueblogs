<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Blog | UniqueBlog</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      background: black;
    }
  </style>
</head>
<body class="min-h-screen flex flex-col text-white font-sans">

  <!-- Navbar -->
  <nav class="bg-gray-950 p-4 shadow-lg">
    <div class="max-w-7xl mx-auto flex justify-between items-center">
      <a href="/">
        <h1 class="text-xl font-bold text-indigo-400 flex items-center gap-2">
          <i class="fas fa-shield-alt text-blue-400"></i>  UniqueBlog
        </h1>
      </a>
      
      <a href="/blogs" class="text-sm md:text-base text-gray-300 hover:text-indigo-300">Back to Blogs</a>
    </div>
  </nav>

  <!-- Edit Blog Form -->
  <main class="flex-grow flex items-center justify-center p-6">
    <div class="w-full max-w-2xl bg-gradient-to-br from-indigo-700 to-purple-700 p-8 rounded-3xl shadow-xl space-y-6">

      <h1 class="text-3xl font-bold mb-4">✏️ Edit Your Blog</h1>

      <form action="/blogs/update/<%= blog._id %>" method="post" class="space-y-4">
        <div>
          <label for="title" class="block text-sm text-white/80 mb-1">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= blog.title %>"
            required
            class="w-full px-4 py-2 rounded-lg text-black focus:outline-none focus:ring-2 focus:ring-indigo-400"
          />
        </div>

        <div>
          <label for="content" class="block text-sm text-white/80 mb-1">Content</label>
          <textarea
            id="content"
            name="content"
            rows="6"
            required
            class="w-full px-4 py-2 rounded-lg text-black resize-none focus:outline-none focus:ring-2 focus:ring-indigo-400"
          ><%= blog.content %></textarea>
        </div>

        <div class="flex justify-end">
          <button
            type="submit"
            class="bg-indigo-500 hover:bg-indigo-600 text-white px-6 py-2 rounded-lg transition duration-200"
          >
            💾 Save Changes
          </button>
        </div>
      </form>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-950 text-white text-center p-4 border-t border-gray-800">
    <p class="text-sm text-gray-400">&copy; 2025 UniqueBlog. All rights reserved.</p>
  </footer>

</body>
</html>
