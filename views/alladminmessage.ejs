<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>User Messages | Admin Panel</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    * {
      scroll-behavior: smooth;
    }
    .custom-scrollbar::-webkit-scrollbar {
      width: 4px;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
      background-color: rgba(129, 140, 248, 0.4);
      border-radius: 4px;
    }
  </style>
</head>

<body class="bg-black text-white font-sans min-h-screen flex flex-col">
  <!-- Header -->
  <header class="bg-gray-900 p-6 shadow-md flex justify-between items-center">
    <h1 class="text-2xl font-bold text-indigo-400"><i class="fas fa-envelope-open-text mr-2"></i>All User Messages</h1>
    <a href="/admin" class="text-indigo-300 hover:text-white transition text-sm"><i class="fas fa-arrow-left mr-1"></i>Back to Dashboard</a>
  </header>

  <!-- Content -->
  <main class="flex-1 p-6">
    <div class="bg-gray-900 rounded-xl p-6 shadow-lg">
      <h2 class="text-xl font-semibold text-indigo-300 mb-4">User Feedback / Contact Messages</h2>
      <div class="overflow-x-auto custom-scrollbar">
        <table class="min-w-full table-auto text-sm text-left border-collapse">
          <thead class="bg-gray-800 text-indigo-200">
            <tr>
              <th class="p-3">#</th>
              <th class="p-3">Username</th>
              <th class="p-3">Email</th>
              <th class="p-3">Message</th>
              <th class="p-3">Date</th>
            </tr>
          </thead>
          <tbody class="text-white divide-y divide-gray-700">
            <!-- Dummy Data -->
           <% allm.forEach(element => { %>
            <tr>
                <td class="p-3">1</td>
                <td class="p-3"><%= element.name %></td>
                <td class="p-3"><%= element.email %></td>
                <td class="p-3"><%= element.message %></td>
                <td class="p-3"><%=new Date(element.createdAt).toLocaleString()  %> </td>
              </tr>
           <% }) %>
           
          </tbody>
        </table>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-900 text-gray-400 text-sm px-6 py-4 text-center rounded-t-2xl shadow-inner mt-10">
    <div class="flex flex-col sm:flex-row justify-between items-center max-w-7xl mx-auto space-y-2 sm:space-y-0">
      <span>&copy; 2025 UniqueBlog Admin Panel. All rights reserved.</span>
      <div class="space-x-4">
        <a href="#" class="hover:text-indigo-400 transition duration-200">Privacy Policy</a>
        <a href="#" class="hover:text-indigo-400 transition duration-200">Terms</a>
        <a href="mailto:support@uniqueblog.com" class="hover:text-indigo-400 transition duration-200">Support</a>
      </div>
    </div>
  </footer>
</body>
</html>
