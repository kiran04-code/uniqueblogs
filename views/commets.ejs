<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Your Blog Comments</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen p-6">

  <h1 class="text-3xl font-bold mb-6 text-purple-400">Comments on Your Posts</h1>
  <% if (locals.coment) { %>
    <h2 class="text-xl font-semibold text-white mb-2">
      Post Title: <span class="text-purple-400"><%= posttitl.title %></span>
    </h2>
    <% coment.forEach(element => { %>
      <div class="mb-4">
        <div class="bg-zinc-800 border-l-4 border-purple-500 p-4 rounded shadow">
          <p class="text-sm text-gray-300">
            <span class="text-purple-300 font-medium"><%= element.createdBy.username %></span>: 
            <%= element.coments %>
          </p>
        </div>
      </div>
    <% }) %>
  
  <% } else { %>
    <p class="text-gray-400 italic">No comments yet. Be the first to share your thoughts!</p>
  <% } %>
  
  
  <!-- Blog Post Section -->
  
</body>
</html>
